const Title = ({ text }) => {
    return (
        <h1>{text}</h1>
    )
}

/* Con este componente evitamos repetir en cada page los html h1. Se podría hacer lo mismo con la imagen u otra cosa. */

export default Title